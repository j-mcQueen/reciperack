import add from "./add.svg";
import build from "./build.svg";
import search from "./search.svg";

interface Icons {
  add: string;
  build: string;
  search: string;
}

const icons: Icons = {
  add,
  build,
  search,
};

export default icons;
